# 交互式数据分析系统项目报告大纲

## 1. 项目背景与实验目标

- 说明数据分析系统在数据读取、清洗、分析和可视化中的作用。
- 说明本项目的实验目标：完成一个可交互的数据分析 Web 系统。
- 强调系统流程：数据上传 → 数据预览 → 数据清洗 → 数据分析 → 数据可视化 → 结果导出。

## 2. 数据集介绍

- 数据来源：`sample_data/Weather_Data.csv`。
- 数据类型：天气观测数据。
- 主要字段：温度、降雨量、湿度、气压、风速、是否下雨等。
- 字段解释示例：
  - `MinTemp`：当日最低温度。
  - `MaxTemp`：当日最高温度。
  - `Rainfall`：降雨量。
  - `Humidity9am`：上午 9 点湿度。
  - `Humidity3pm`：下午 3 点湿度。
  - `RainToday`：今天是否下雨。
  - `RainTomorrow`：明天是否下雨。

## 3. 系统需求分析

- 支持 CSV、XLSX、XLS 文件上传。
- 支持数据预览和字段说明。
- 支持缺失值、重复值、异常值和字段类型处理。
- 支持基础统计分析和 KMeans 聚类分析。
- 支持聚类质量评价、肘部法和 PCA 可视化。
- 支持多种 Plotly 交互式图表。
- 支持清洗结果和分析结果导出。
- 对非法上传、空数据、无数值列、字段选择错误等情况给出友好提示。

## 4. 系统总体设计

- 后端框架：Flask。
- 数据处理：Pandas、NumPy。
- 机器学习分析：Scikit-learn。
- 可视化：Plotly。
- 页面展示：HTML、CSS、Jinja2 模板。
- 模块化设计：
  - `app.py`：路由整合。
  - `modules/io_utils.py`：文件读取。
  - `modules/dataset_info.py`：数据集说明。
  - `modules/clean.py`：数据清洗。
  - `modules/analysis.py`：统计分析和聚类。
  - `modules/visualize.py`：图表生成。

## 5. 系统功能模块设计

### 5.1 数据上传模块

- 文件类型限制：`.csv`、`.xlsx`、`.xls`。
- 文件大小限制：10MB。
- 使用 `secure_filename` 处理上传文件名。
- CSV 多编码读取，提高中文数据兼容性。

### 5.2 数据预览模块

- 展示文件名、行数、列数和字段列表。
- 展示前 20 行数据。
- 表格支持横向滚动，避免页面溢出。

### 5.3 数据集说明模块

- 展示数值字段、类别字段、日期字段数量。
- 展示缺失值总数和重复行数量。
- 天气数据自动显示字段解释。
- 普通数据展示字段名、类型、非空数量、缺失数量、缺失率和示例值。

### 5.4 数据清洗模块

- 数值型缺失值处理：均值、中位数、0、删除、不处理。
- 类别型缺失值处理：众数、Unknown、删除、不处理。
- 重复行删除。
- 异常值检测：IQR 四分位距法、Z-Score 标准分法。
- 异常值处理：盖帽、删除、标记、只检测。
- 天气数据专用规则：湿度范围、云量范围、非负字段、RainToday/RainTomorrow 标准化、温度字段修正。
- 清洗前后对比：行列变化、缺失值变化、重复值变化、异常值处理数量和字段类型转换数量。

### 5.5 数据分析模块

- 基础统计：均值、中位数、最小值、最大值、标准差。
- KMeans 聚类：用户选择数值字段和 K 值。
- 轮廓系数：评价聚类质量。
- 肘部法：计算不同 K 值下的 SSE / Inertia。
- PCA 二维降维：展示聚类结果分布。

### 5.6 数据可视化模块

- 默认图表：
  - 天气温度趋势折线图。
  - 降雨占比饼图。
  - 湿度与温度散点图。
  - 数值字段相关性热力图。
  - 缺失值柱状图。
  - 直方图。
  - 箱线图。
- 自定义图表：
  - 柱状图。
  - 折线图。
  - 饼图。
  - 散点图。
  - 直方图。
  - 箱线图。
  - 缺失值柱状图。

### 5.7 结果导出模块

- 导出清洗后数据：`data/cleaned_data.csv`。
- 导出分析后数据：`data/analyzed_data.csv`。
- 未生成结果时给出提示。

## 6. 关键技术实现

### 6.1 Flask 路由设计

- `/`：首页和文件上传入口。
- `/upload`：处理上传文件。
- `/preview`：数据预览。
- `/dataset_info`：数据集说明。
- `/clean`：数据清洗。
- `/analysis`：数据分析。
- `/visual`：数据可视化。
- `/exports`：结果导出。
- `/download_cleaned`：下载清洗结果。
- `/download_analyzed`：下载分析结果。

### 6.2 Pandas 数据处理

- 读取 CSV / Excel。
- 自动处理缺失值、重复值和字段类型。
- 生成统计摘要和清洗报告。

### 6.3 缺失值和异常值处理

- 缺失值按字段类型选择不同处理策略。
- IQR 和 Z-Score 用于异常值检测。
- 支持盖帽、删除、标记和只检测。

### 6.4 KMeans 聚类分析

- 对数值特征进行标准化。
- 使用 `KMeans(n_clusters=k, random_state=42, n_init=10)`。
- 将聚类结果写入 `cluster` 字段。

### 6.5 轮廓系数和肘部法

- 使用 `silhouette_score` 评价聚类质量。
- 计算 K=2 到 K=8 的 SSE / Inertia，绘制肘部法图。

### 6.6 PCA 降维可视化

- 使用 PCA 将多维特征降到二维。
- 绘制 PC1 和 PC2 散点图，并按聚类类别着色。

### 6.7 Plotly 交互式图表

- 使用 Plotly Express 生成图表。
- 图表以 HTML 片段形式嵌入页面。
- 支持鼠标悬停查看数据详情。

## 7. 系统运行截图

建议截图顺序：

1. 首页上传界面。
2. 数据预览页面。
3. 数据集说明页面。
4. 数据清洗配置页面。
5. 清洗前后对比页面。
6. 数据分析页面。
7. 肘部法图。
8. PCA 聚类散点图。
9. 数据可视化页面。
10. 结果导出页面。

## 8. 小组分工

| 人员 | 角色 | 主要负责内容 | 对应模块/文件 |
|---|---|---|---|
| 组长 | 项目负责人 / 后端整合 | 负责整体进度安排、功能协调、Flask 路由整合、系统运行调试、答辩汇报与文档统稿 | `app.py`、README、项目文档 |
| 组员 1 | 数据上传与读取模块负责人 | 负责 CSV/Excel 文件上传、文件格式校验、编码兼容处理、数据读取与基础预览功能 | `modules/io_utils.py`、`templates/index.html`、`templates/preview.html` |
| 组员 2 | 数据清洗模块负责人 | 负责缺失值处理、重复值删除、异常值检测、字段类型转换、天气数据专用清洗规则、清洗报告生成、清洗前后对比 | `modules/clean.py`、`templates/clean.html` |
| 组员 3 | 数据分析模块负责人 | 负责基础统计分析、KMeans 聚类、轮廓系数、肘部法、PCA 聚类可视化、分析结果导出 | `modules/analysis.py`、`templates/analysis.html` |
| 组员 4 | 数据可视化模块负责人 | 负责 Plotly 图表生成，包括折线图、柱状图、饼图、散点图、热力图、直方图、箱线图、缺失值图 | `modules/visualize.py`、`templates/visual.html` |
| 组员 5 | 前端页面与测试文档负责人 | 负责页面布局、美化、交互提示、系统测试、运行截图整理、问题记录与个人/小组文档补充 | `templates/`、`static/css/style.css`、测试截图、实验报告 |

## 9. 测试结果

建议记录：

- 示例数据是否可以正常上传。
- 数据预览是否正常。
- 数据集说明是否正确识别字段。
- 数据清洗是否生成对比报告。
- KMeans 聚类是否生成 `cluster` 字段。
- 轮廓系数是否显示。
- 肘部法图是否显示。
- PCA 聚类图是否显示。
- 默认图表和自定义图表是否显示。
- 清洗结果和分析结果是否可以下载。

## 10. 遇到的问题与解决方案

- CSV 文件编码不一致：通过多编码顺序尝试读取解决。
- 字段类型复杂：通过日期字段识别和数值字段自动转换解决。
- 缺失值和异常值影响分析结果：通过清洗策略和清洗前后对比解决。
- K 值不合理：通过范围限制、样本数量校验、轮廓系数和肘部法辅助解决。
- 页面表格过宽：通过横向滚动和固定表头解决。

## 11. 项目不足与改进方向

- 当前系统主要面向本地单机课程演示，暂未实现用户登录和多用户数据隔离。
- 后续可以引入 Flask session、数据库和用户管理模块。
- 可以增加历史数据集管理和历史分析记录。
- 可以增加更多机器学习模型和图表导出功能。

## 12. 个人收获

- 熟悉 Flask Web 应用开发流程。
- 掌握 Pandas 数据读取、清洗和统计分析方法。
- 理解 KMeans 聚类、轮廓系数、肘部法和 PCA 的应用场景。
- 学会使用 Plotly 构建交互式数据可视化页面。
- 提升模块化编程、异常处理和项目文档撰写能力。
